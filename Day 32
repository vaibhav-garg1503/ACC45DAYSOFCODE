#include <bits/stdc++.h>
using namespace std;

int main(){
    int t,x,y,z;
    cin >> t;
    while (t--)
    {
        cin >> x;
        cin >> y;
        cin >> z;
        int arr[3] = {x, y, z};
        sort(arr, arr + 3);
        cout << arr[1] << endl;
    }
    return 0;
}
